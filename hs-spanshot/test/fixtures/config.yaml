capture:
  pre_window_duration: 10
  post_window_duration: 3
  min_context_events: 5
  max_post_window_events: 1000
  detection_rules:
    - regex_pattern: "ERROR"
    - regex_pattern: "WARN"
    - regex_pattern: "FATAL"
